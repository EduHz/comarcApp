import { View, Text } from "react-native";

export default function Perfil() {
  return (
    <View style={{ padding: 20 }}>
      <Text style={{ fontSize: 20 }}>Perfil del usuario</Text>
      {/* Login, logout, datos del usuario */}
    </View>
  );
}
